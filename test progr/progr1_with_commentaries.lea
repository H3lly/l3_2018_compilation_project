/* **************************************************
 This file is about the same as progr1.lea, except I added commentary what are each line
(var, procedures, integer, types, etc.) so we better understand how lea language works,
and thus doesn't have anything to do with the project in itself.
 *
 ************************************************** */

type

caractere = character;
entier = integer;
booleen = boolean;

Months = (January, February, March, April, May, June, July, August, September, October, November, December);
DaysOfWeek = (Sunday, Monday, Tuesday, Wednesday, Thursday, Friday, Saturday);

range1 = 64 .. 81;
range2 = Monday .. Friday;
range3 = Monday .. Thursday;

table1 = array [ 1 .. 32 ] of array [ range3 ] of integer;

ptr = ^integer;

var

x, y, z : integer;
b : boolean;
z2 : array [ 1 .. 32] of integer;
k : array [ range2 ] of integer;
l : array [ range2 ] of boolean;
m2 : array [ Monday .. Thursday ] of boolean;
p : ptr;
m : Months;
r1: range1;
r2: range2;
x2 : boolean;


function goo(i : integer, j : integer) : boolean;

function foo(i : integer, j : integer) : ^boolean
var
	result : ^boolean;
begin
	new(result);
	result^ := goo(i, j);
	return (result);
end

function goo(i : integer, j : integer) : boolean
begin
	return (i+j);
end



begin
	b := false;
	b := foo(3, 7)^;
	b := goo(3, 7);
	
	z2 [ Thursday ] := 36;
	x := z2 [ Thursday ];
	b := goo(z2 [ Thursday ], 1);
	

	if (l [ Monday ]) then x := 1;

	x := -(36 + 45 / 7);

	p := null;
	new (p);
	p^ := 36;
	x := p^;
	dispose (p);
	

end
